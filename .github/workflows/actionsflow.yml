name: YouTube Canli Yayin Kontrol
on:
  schedule:
    - cron: "*/5 * * * *" # Her 5 dakikada bir kontrol eder
  workflow_dispatch: # Manuel başlatma butonu ekler

jobs:
  check_live:
    runs-on: ubuntu-latest
    steps:
      - name: YouTube Canlı Yayın Kontrolü
        run: |
          CHANNEL_ID="UC_p0BvLhK0H8WvHjX9T-q9A" 
          RESPONSE=$(curl -s "https://www.youtube.com/channel/$CHANNEL_ID/live")
          
          if echo "$RESPONSE" | grep -q '{"text":" canlı"}'; then
            echo "Yayın aktif! Telegram bildirimi gönderiliyor..."
            curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage \
              -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
              -d text="🔴 YAYIN BAŞLADI! Instagram 30K kitlene bu linki gönder: https://www.youtube.com/channel/$CHANNEL_ID/live"
          else
            echo "Henüz yayın yok."
          fi
